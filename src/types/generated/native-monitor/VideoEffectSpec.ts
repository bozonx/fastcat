// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

export type VideoEffectSpec = { "type": "gaussian-blur", radius: number, 
/**
 * When true, the frame is padded so the blur can bleed past the
 * original rectangle into transparency — needed for PNGs / cutouts to
 * blur "off the edge" naturally. When false (the default), no padding
 * is added and the blur clamps to the frame edges, so opaque video
 * keeps a full-bleed, crisp border instead of darkening/fading.
 */
bleed: boolean, blur_type: string, mix: number, } | { "type": "gaussian-blur-pixels", radius: number, mix: number, } | { "type": "brightness", value: number, } | { "type": "contrast", value: number, } | { "type": "saturation", value: number, } | { "type": "hue", degrees: number, } | { "type": "levels", in_black: number, in_white: number, gamma: number, out_black: number, out_white: number, mix: number, } | { "type": "chroma-key", key_rgba: [number, number, number, number], threshold: number, smoothness: number, } | { "type": "sharpen", amount: number, mix: number, } | { "type": "pixelate", size: number, mix: number, } | { "type": "bloom", threshold: number, strength: number, radius: number, knee: number, mix: number, } | { "type": "vignette", strength: number, radius: number, softness: number, mix: number, } | { "type": "noise", amount: number, seed: number, noise_type: string, scale: number, mix: number, } | { "type": "chromatic-aberration", amount: number, angle_deg: number, mix: number, } | { "type": "color-tone", color_rgba: [number, number, number, number], amount: number, blend_mode: string, preserve_luminance: boolean, range: string, } | { "type": "blur-fill", 
/**
 * Foreground (sharp video) scale; 1.0 = contain-fit.
 */
fg_scale: number, 
/**
 * Background zoom on top of cover-fit; 1.0 = just cover the frame.
 */
bg_scale: number, 
/**
 * Background blur radius in px @1080p.
 */
blur: number, 
/**
 * Background brightness multiplier; 1.0 = no dim.
 */
bg_dim: number, 
/**
 * Background saturation; 1.0 = normal, 0 = grayscale.
 */
bg_saturation: number, 
/**
 * Background tint colour (RGBA; alpha ignored). Mixed into the
 * background by `tint_strength`.
 */
tint_color: [number, number, number, number], 
/**
 * Background tint strength; 0 = no tint, 1 = fully the tint colour.
 */
tint_strength: number, 
/**
 * Foreground vertical offset as a fraction of frame height (−0.5..0.5).
 */
fg_offset_y: number, } | { "type": "custom-wgsl", source: string, params: Record<string, unknown>, };
